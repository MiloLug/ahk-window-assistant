; here you can use %ENV_VARS% - the get auto expanded (look at launcher.logPath)

ahkPath = C:\Program Files\AutoHotkey\v2\AutoHotkey64.exe ; Make sure this is not writable by a non-admin

[setup]
; Deploy destination for scripts\Setup.ps1 (see notes\elevated-autostart.md). Must not be writable by the non-admin account.
path = C:\Program Files\AhkWindowAssistant ; Make sure this is not writable by a non-admin 

[launcher]
logPath = %SystemRoot%\Temp\AhkWindowAssistant-launcher.log
